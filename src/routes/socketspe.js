const {addUser,getUser} = require('./manage')
module.exports = (io) => {
    // io.on('connection',(socket) => {
    //     console.log('Have a connection ' + socket.id)
    //     socket.on("getQR",(data) =>{
    //         console.log(data)
    //        // io.emit('getReload', 'I just met you');
    //      //  io.to()
    //     })
        
    // })

    
}